apiVersion: v2
name: cloudflare-tunnel-ingress-controller
description: Publishes Kubernetes Ingresses through a Cloudflare Tunnel, managing the tunnel's routes, DNS records and cloudflared connectors
type: application
home: https://github.com/clbs-io/cloudflare-tunnel-ingress-controller
sources:
  - https://github.com/clbs-io/cloudflare-tunnel-ingress-controller
keywords:
  - cloudflare
  - cloudflare-tunnel
  - cloudflared
  - ingress
  - ingress-controller
# The two versions below are placeholders; the publish workflow sets both from
# the release tag.
version: 0.4.0
appVersion: 0.0.0
